import React, { Component } from 'react';

import {
    ContentWrapper,
} from './styled';

export default class Content extends Component {
    render() {
        return (
            <ContentWrapper />
        );
    }
} 